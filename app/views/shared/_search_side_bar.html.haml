#search-sidebar.col-md-3
  .search-container
    %h4= t('adeca.search.search_properties')
    = form_tag(search_index_path, method: 'get') do
      = text_field_tag :iwano
      %br
      = submit_tag t('adeca.search.search_submit')
